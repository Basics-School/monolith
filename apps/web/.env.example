# =============================================================================
# WEB APPLICATION ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and update the values according to your setup
#
# This file contains environment variables specific to the Next.js web application
# For the main application environment variables, see the root .env.example file

# =============================================================================
# NEXT.JS APPLICATION SETTINGS
# =============================================================================

# Your application's public URL (accessible from the browser)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Root domain for cross-subdomain cookies (used by Better Auth)
ROOT_DOMAIN=localhost

# =============================================================================
# SUPABASE CONFIGURATION (if using Supabase Auth)
# =============================================================================

# Supabase Project URL (publicly accessible)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:8000

# Supabase Anonymous Key (safe to expose in client-side code)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# =============================================================================
# BETTER AUTH CONFIGURATION (if using Better Auth)
# =============================================================================

# Better Auth Secret Key (32+ characters, keep this secure!)
BETTER_AUTH_SECRET=your-better-auth-secret-32-chars-minimum

# Better Auth URL (optional, defaults to NEXT_PUBLIC_SITE_URL)
# BETTER_AUTH_URL=http://localhost:3000

# =============================================================================
# DATABASE CONNECTION (if connecting directly from the web app)
# =============================================================================

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:your-password@localhost:5432/postgres

# =============================================================================
# REDIS CONFIGURATION (required for Better Auth sessions)
# =============================================================================

# Upstash Redis REST URL
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io

# Upstash Redis REST Token
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-token

# =============================================================================
# DEVELOPMENT vs PRODUCTION NOTES
# =============================================================================

# Development Setup:
# - Use .env.local for local development
# - The values above work with the default docker-compose setup
# - Supabase runs on http://localhost:8000
# - Web app runs on http://localhost:3000

# Production Setup:
# - Update all URLs to your production domains
# - Use secure HTTPS URLs
# - Generate new secret keys
# - Use production database credentials
# - Update CORS settings in Supabase if needed

# Security Notes:
# - NEVER commit actual secrets to version control
# - Use different secrets for each environment
# - Rotate secrets regularly
# - Only NEXT_PUBLIC_* variables are exposed to the browser
